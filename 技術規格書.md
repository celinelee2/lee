# 點名表系統 — 技術規格書（v1）

> 本文件可直接交給 Claude Code 或工程師，作為從零建立專案的依據。
> GitHub repo：`https://github.com/celinelee2/lee.git`（程式碼初始化後請 push 到這裡）

## 1. 專案概述

到校上課老師用的點名系統。一位老師可帶多個課程時段（每週固定某天某時段、某校區、某科目上課），每堂課的學生名單由管理者指定，可能來自不同的班別混合編成。老師登入後可針對「今天的課」標記每位學生出席或請假，並能查看/匯出該課程時段的月報表。管理者在後台維護老師帳號、課程時段/班別/學生資料，以及學生與課程時段的分配關係。

v1 範圍只做點名功能，作品上傳留待 v2。

## 2. 技術棧

| 項目 | 選擇 | 原因 |
|---|---|---|
| 前端 + 後端 | Next.js（App Router） | 前後端同一專案，部署到 Zeabur 簡單，Server Components 適合這種表單為主的應用 |
| 資料庫 | PostgreSQL | 關聯式資料、報表查詢（月結統計）用 SQL 很自然 |
| ORM | Prisma | 型別安全、migration 工具成熟、與 Next.js 整合度高 |
| 認證 | Auth.js（NextAuth v5）+ credentials provider | 自架帳密登入，不需要串第三方登入 |
| Email 服務 | Resend（或 SendGrid） | 串接簡單，免費額度夠用於忘記密碼信、通知信 |
| 部署 | Zeabur（含 PostgreSQL 服務） | 需求指定 |
| 版本控制 | GitHub | 需求指定 |
| UI 樣式 | Tailwind CSS | 開發快、可直接套用木質日式色票 |

## 3. 資料庫 Schema

```prisma
// schema.prisma

enum Role {
  ADMIN
  TEACHER
}

enum AttendanceStatus {
  PRESENT   // 出席
  ABSENT    // 請假
}

model User {
  id           String   @id @default(cuid())
  email        String   @unique
  passwordHash String
  name         String
  role         Role     @default(TEACHER)
  isActive     Boolean  @default(true)   // 停用帳號用，不做硬刪除
  createdAt    DateTime @default(now())

  classes      ClassTeacher[]
  resetTokens  PasswordResetToken[]
}

model PasswordResetToken {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  token     String   @unique
  expiresAt DateTime
  usedAt    DateTime?
  createdAt DateTime @default(now())
}

// 全系統層級的固定設定，目前只有一筆資料（機構名稱等）
// 管理者後台可編輯，前台點名表表頭讀取顯示
model OrgSetting {
  id           String   @id @default(cuid())
  orgName      String   @default("Artpresso國際教育中心")
  updatedAt    DateTime @updatedAt
}

// 課程時段：點名表的主體單位。固定上課校區、科目、星期幾時段、起訖日。
// 不直接擁有學生名單，學生透過 ClassStudent 被分配進來，可能來自不同班別混合編成。
model Class {
  id          String   @id @default(cuid())
  name        String              // 課程時段名稱，例如「週三素描班」，方便老師/管理者辨識
  campus      String              // 上課校區，唯一的場地資訊（老師視角不需要區分學校/校區，例如「健康國小信義分部」）
  subject     String              // 上課科目，自由文字（例如「素描」「水彩」）
  weekday     Int                 // 0=週日 ... 6=週六，固定每週上課的星期幾
  scheduleText String             // 上課時程顯示文字，例如「週三 16:00-17:00」
  startDate   DateTime @db.Date   // 上課起日
  endDate     DateTime @db.Date   // 上課訖日
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())

  teachers      ClassTeacher[]
  studentLinks  ClassStudent[]
  sessions      ClassSession[]
}

// 老師與課程時段多對多（一位老師可帶多個時段，一個時段也可能有代課老師）
model ClassTeacher {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  classId   String
  class     Class    @relation(fields: [classId], references: [id])
  createdAt DateTime @default(now())

  @@unique([userId, classId])
}

// 班別：純粹的標籤名單（不裝學生上課邏輯），例如「3年級A班」
// 由管理者維護，可新增/停用；每位學生必填一個班別
model EnrollmentLabel {
  id        String   @id @default(cuid())
  name      String              // 例如「3年級A班」，前台顯示為「班別」
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())

  students  Student[]
}

// 學生主檔，獨立存在，不綁定單一課程時段
model Student {
  id                String          @id @default(cuid())
  name              String
  studentNumber     String?             // 學號，可選；學校沒有提供學號時留空，前端自動以序號顯示
  enrollmentLabelId String
  enrollmentLabel   EnrollmentLabel @relation(fields: [enrollmentLabelId], references: [id])
  isActive          Boolean  @default(true)   // 停用＝整體退出系統，不做硬刪除（保留歷史點名記錄）
  createdAt         DateTime @default(now())

  classLinks   ClassStudent[]
  attendances  Attendance[]
}

// 學生與課程時段的分配關係（多對多）：一位學生可同時被分配到多個課程時段
// 例如同一學生：週二上素描班、週五上水彩班
model ClassStudent {
  id        String   @id @default(cuid())
  classId   String
  class     Class    @relation(fields: [classId], references: [id])
  studentId String
  student   Student  @relation(fields: [studentId], references: [id])
  isActive  Boolean  @default(true)   // 停用＝該生退出這個課程時段，不影響其他時段
  createdAt DateTime @default(now())

  @@unique([classId, studentId])
}

// 代表「某課程時段某天的一堂課」，點名表的單位
model ClassSession {
  id         String   @id @default(cuid())
  classId    String
  class      Class    @relation(fields: [classId], references: [id])
  sessionDate DateTime @db.Date     // 上課日期（只存日期）
  completedAt DateTime?             // 老師按下「完成點名」的時間，null 代表尚未完成
  createdAt  DateTime @default(now())

  attendances Attendance[]

  @@unique([classId, sessionDate])   // 同一課程時段同一天只會有一筆 session
}

model Attendance {
  id         String           @id @default(cuid())
  sessionId  String
  session    ClassSession     @relation(fields: [sessionId], references: [id])
  studentId  String
  student    Student          @relation(fields: [studentId], references: [id])
  status     AttendanceStatus
  markedAt   DateTime         @default(now())
  markedBy   String           // userId of teacher who marked it

  @@unique([sessionId, studentId])
}
```

### 設計重點說明

- **不做硬刪除**：課程時段、班別、學生、老師帳號都用 `isActive` 標記停用，避免刪除後歷史點名資料變成孤兒資料，月報表才能往前回溯。
- **Class（課程時段）是點名表的主體**：固定校區、科目、星期幾時段、起訖日，但不直接「擁有」學生名單——學生名單透過 `ClassStudent` 分配進來，可能混合多個班別的學生。
- **ClassStudent 是混班點名的核心**：一位學生可同時出現在多個課程時段（例如週二素描班、週五水彩班），停用某筆 `ClassStudent` 只代表該生退出這一個時段，不影響其他時段的分配。
- **EnrollmentLabel（前台顯示為「班別」）是純標籤名單**：獨立管理（可新增/停用），不裝學生上課邏輯，每位學生必填一個班別。點名表學生清單中，每位學生姓名旁只顯示這個「班別」標籤（例如「3年級A班」），其他課程資訊（校區、科目、老師等）統一放在表頭區塊。
- **ClassSession 是點名的核心單位**：「課程時段＋日期」唯一決定一筆 session，老師進入點名頁時，系統用 `upsert` 自動建立或找到當天的 session，學生名單則從當下有效的 `ClassStudent` 關係讀取。
- **Attendance 記錄誰標記、何時標記**：方便日後追溯或除錯（例如代課老師點名）。
- **weekday 欄位**目前只作顯示用（提示老師今天是否為上課日），v1 不強制檢查，避免補點名時被卡住。
- **OrgSetting**：機構名稱（如「Artpresso國際教育中心」）是全系統共用的單一設定，不綁定在課程時段上，管理者後台可編輯，前台點名表表頭最上方讀取顯示。
- **表頭資訊的顯示順序**：機構名稱（標題）→ 上課校區（`campus`）→ 上課科目（`subject`）→ 上課老師 → 上課時程（`scheduleText`）→ 起訖日期（`startDate`/`endDate`）。這是老師視角的點名表，不需要區分「學校」與「校區」兩層概念，`campus` 即為唯一的場地資訊。
- **campus（上課校區）**：自由文字欄位，由管理者在課程時段設定中編輯，可包含學校與分部資訊（例如「健康國小信義分部」），不再拆成獨立的 `School` 模型。
- **subject（上課科目）**：與 `campus` 同樣是 `Class` 底下固定的文字欄位（例如「素描」「水彩」），由管理者在課程時段設定中編輯，點名表表頭直接顯示。
- **studentNumber（學號/序號）**：管理者新增學生時可填入學校提供的學號；若留空，前端依該生在點名表中的排列順序自動產生序號（1、2、3...）顯示在最左側。兩者統一顯示在同一欄位，學號優先，序號為退而求其次的顯示。
- **scheduleText / startDate / endDate**：上課時程以顯示文字存（例如「週三 16:00-17:00」），避免額外解析格式的複雜度；起訖日則用兩個日期欄位存課程時段的開課與結課日期，皆在管理者後台維護。

## 4. 頁面與路由規劃

```
/login                          登入頁
/forgot-password                忘記密碼（輸入 email）
/reset-password/[token]         重設密碼

/(teacher)/                     老師登入後首頁：課程時段列表
/(teacher)/class/[classId]      點名頁（預設今天，可切換日期補點名）
/(teacher)/class/[classId]/report   月報表查看與匯出（CSV）

/(admin)/admin                  管理者後台首頁
/(admin)/admin/settings         系統設定（機構名稱等全系統設定）
/(admin)/admin/users            老師帳號管理（新增/停用/刪除）
/(admin)/admin/classes          課程時段管理（含批次新增、指派老師、科目/校區/時程/起訖日設定）
/(admin)/admin/enrollment-labels   班別管理（新增/停用）
/(admin)/admin/students         學生名單管理（含批次新增、設定班別、分配至課程時段）
```

### 權限規則
- `TEACHER` 只能存取自己被指派（`ClassTeacher`）的課程時段，其餘回 403。
- `ADMIN` 可存取所有 `/admin/*` 路徑；一般老師無法進入。
- 用 middleware 統一檢查 session + role，避免每個 page 各自寫判斷邏輯。

## 5. 核心 API 設計

| Method | Path | 說明 |
|---|---|---|
| POST | `/api/auth/login` | 登入（Auth.js 內建） |
| POST | `/api/auth/forgot-password` | 寄送重設密碼信 |
| POST | `/api/auth/reset-password` | 用 token 設定新密碼 |
| GET | `/api/classes` | 取得目前登入老師的課程時段列表 |
| GET | `/api/classes/:id/session?date=YYYY-MM-DD` | 取得該課程時段該日點名表（含學生名單、序號/學號、班別標籤、目前標記狀態、表頭資訊：機構名/上課校區/科目/起訖日/上課時程/老師姓名） |
| POST | `/api/classes/:id/session/:sessionId/attendance` | 標記單一學生出席/請假（body: studentId, status） |
| POST | `/api/classes/:id/session/:sessionId/complete` | 標記點名完成 |
| GET | `/api/classes/:id/report?month=YYYY-MM` | 月報表 JSON |
| GET | `/api/classes/:id/report/export?month=YYYY-MM` | 月報表 CSV 下載 |
| GET/PATCH | `/api/admin/settings` | 取得/編輯系統設定（機構名稱，限 ADMIN） |
| GET/POST/PATCH/DELETE | `/api/admin/users` | 老師帳號 CRUD（限 ADMIN） |
| GET/POST/PATCH/DELETE | `/api/admin/classes` | 課程時段 CRUD（含科目、上課校區、上課時程文字、起訖日），含批次匯入 |
| GET/POST/PATCH/DELETE | `/api/admin/enrollment-labels` | 班別 CRUD（新增/停用） |
| GET/POST/PATCH/DELETE | `/api/admin/students` | 學生 CRUD（含學號可留空、必填班別），含批次匯入（CSV 貼上或上傳） |
| POST/DELETE | `/api/admin/classes/:id/students` | 將學生分配進/移出某課程時段（操作 `ClassStudent`） |

### 點名標記的行為
1. 老師進入 `/class/[classId]`，前端打 `GET /api/classes/:id/session?date=今天`。
2. 後端用 `(classId, sessionDate)` upsert 一筆 `ClassSession`，回傳該課程時段目前所有 `isActive` 的 `ClassStudent` 分配學生 + 各自目前的 attendance 狀態（未標記則為 null）+ 各自的班別標籤。
3. 老師點「出席」或「請假」→ 前端立即呼叫 `POST .../attendance`，後端 upsert `Attendance`（`sessionId + studentId` 唯一），即時生效，不需額外確認步驟（依你的需求確認）。
4. 全部標記完按「完成點名」→ 呼叫 `complete`，寫入 `completedAt`，可選擇觸發 Email 通知管理者「XX老師已完成XX課程時段點名」。

## 6. Email 通知情境（v1）

| 情境 | 收件人 | 觸發時機 |
|---|---|---|
| 忘記密碼 | 老師本人 | 點擊「忘記密碼」送出 email |
| 新帳號建立 | 新老師 | 管理者後台新增老師帳號時，附帶初始密碼設定連結 |
| 點名完成通知（可選） | 管理者 | 老師按下「完成點名」 |

> 點名完成通知是否需要，建議先不做，避免管理者信箱被灌爆；之後若有需求再開。

## 7. 月報表（CSV 匯出）格式

```
學生姓名,班別,出席次數,請假次數,應上課次數
王小明,3年級A班,3,1,4
陳美琪,4年級B班,4,0,4
```

「應上課次數」＝ 該月內，課程時段 `weekday` 對應的星期共出現幾次（協助核對是否有缺漏點名的課堂）。

## 8. 視覺設計規範

延續已確認的視覺稿：

- 背景：暖米色 `#F7F2EA`
- 主要強調色（按鈕、標題）：木質棕 `#8A6D3F` / 深棕文字 `#4A3B2A`
- 卡片：白底 `#FFFFFF`，淺棕邊框 `#E0D5C2`
- 狀態色：出席用綠 `#3B6D11`，請假用紅 `#A32D2D`，未點用灰棕 `#8A7A63`
- 圓角：卡片 `12px`，按鈕 `8px`
- 字體：系統預設無襯線字體即可，避免過度裝飾

## 9. 部署流程（Zeabur）

> GitHub repo：`https://github.com/celinelee2/lee.git`

1. 將專案程式碼 push 到上述 GitHub repo，`main` branch 為部署來源。
2. Zeabur 建立專案，新增 PostgreSQL 服務（取得 `DATABASE_URL`）。
3. 新增 Next.js 服務，從上述 GitHub repo 部署，環境變數設定：
   - `DATABASE_URL`
   - `AUTH_SECRET`
   - `RESEND_API_KEY`（或所選 Email 服務金鑰）
   - `NEXTAUTH_URL`（正式網址）
4. 部署時自動執行 `prisma migrate deploy`。
5. 首次部署後，手動執行 seed script 建立第一個管理者帳號（`celinelee.sow@gmail.com`）。

## 10. 開發階段建議順序

1. 專案初始化（Next.js + Prisma + Tailwind）、資料庫 schema 與 migration
2. 登入系統（含管理者 seed 帳號、忘記密碼流程）
3. 管理者後台：課程時段/班別/學生 CRUD（含批次新增）、學生分配至課程時段
4. 老師端：課程時段列表 + 點名表（核心功能，含班別標籤顯示）
5. 月報表查看與 CSV 匯出
6. Email 通知串接
7. 部署上線、邀請老師帳號測試
8. （v2）作品上傳表
